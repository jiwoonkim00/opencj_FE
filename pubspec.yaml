name: opencj_fe
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  barcode_widget: ^2.0.4
  firebase_core: ^3.6.0
  http: ^1.2.2

  # ✅ 네이버 지도 플러그인 추가
  flutter_naver_map: ^1.4.1+1

  # ✅ 런타임 패키지명 확인용
  package_info_plus: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # (선택) 이미지 에셋은 유지
  assets:
    - assets/images/
    # ❌ 더 이상 사용 안 함: assets/kakao_map.html
